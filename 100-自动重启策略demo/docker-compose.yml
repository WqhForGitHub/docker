version: "3.8"
services:
  # 总是重启
  always:
    build: .
    ports: ["3001:3000"]
    restart: always

  # 除非手动停止
  unless-stopped:
    build: .
    ports: ["3002:3000"]
    restart: unless-stopped

  # 失败时重启（最多3次）
  on-failure:
    build: .
    ports: ["3003:3000"]
    restart: on-failure:3
