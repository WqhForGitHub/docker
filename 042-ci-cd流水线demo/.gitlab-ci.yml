stages:
  - test
  - build
  - deploy

test:
  stage: test
  script:
    - npm install
    - npm test

build:
  stage: build
  script:
    - docker build -t ci-cd-demo:$CI_COMMIT_SHORT_SHA .

deploy:
  stage: deploy
  script:
    - docker-compose up -d
  only:
    - main
