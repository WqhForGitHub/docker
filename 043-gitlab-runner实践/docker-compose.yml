version: "3.8"
services:
  runner:
    build: .
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - CI_SERVER_URL=https://gitlab.com/
      - REGISTRATION_TOKEN=YOUR_TOKEN
