version: "3.8"
services:
  app:
    build: .
    ports:
      - "3000:3000"
    deploy:
      replicas: 3
      resources:
        limits:
          cpus: "0.5"
          memory: "128M"
      restart_policy:
        condition: any
    environment:
      - ENV=production
