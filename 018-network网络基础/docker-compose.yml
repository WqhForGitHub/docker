version: "3.8"
services:
  app1:
    image: nginx:alpine
    ports:
      - "8081:80"
    networks:
      - demo-net

  app2:
    image: nginx:alpine
    ports:
      - "8082:80"
    networks:
      - demo-net

networks:
  demo-net:
    driver: bridge
