# 72-爬虫服务容器版

Python 爬虫服务容器化，使用 requests + BeautifulSoup。

## 使用方法

```bash
docker build -t demo-72-spider .
docker run --rm demo-72-spider
```

## 学习要点

- Python 爬虫容器化
- requests + BeautifulSoup
- 定时任务扩展
- 依赖管理
