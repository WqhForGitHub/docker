version: "3.8"
services:
  app:
    build: .
    ports: ["3000:3000"]
    environment:
      - APP_NAME=my-app
      - APP_VERSION=1.0.0
      - DATABASE_URL=postgres://user:pass@db:5432/mydb
      - API_KEY=secret-key-12345
      - DEBUG=true
