# 13-flask入门项目

Flask 入门项目，包含路由和模板渲染。

## 使用方法

```bash
docker build -t demo-13-flask .
docker run -d -p 5000:5000 demo-13-flask
```

访问 http://localhost:5000

## 学习要点

- Flask 路由定义
- 模板渲染
- 全局状态（容器内）
