<source>
  @type forward
  port 24224
</source>

<match nginx.**>
  @type file
  path /fluentd/log/nginx
  append true
  <format>
    @type json
  </format>
</match>

<match **>
  @type stdout
</match>
